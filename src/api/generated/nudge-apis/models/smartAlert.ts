/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * Define Edge Agents API
 * LangGraph-based multi-agent system for financial market analysis
 * OpenAPI spec version: 1.0.0
 */
import type { AlertAssetClass } from "./alertAssetClass";
import type { AlertKind } from "./alertKind";
import type { HoldingRef } from "./holdingRef";
import type { NudgeBadge } from "./nudgeBadge";
import type { SmartAlertMeta } from "./smartAlertMeta";

/**
 * One alert: a verdict on one holding, with the figures that earned it.

The badge, meta and chat question are deterministic templates over provider
figures. The line is written by the LLM, grounded only on the facts the rule
computed, and falls back to a template when that call fails or times out —
so it can only say what the data says.
 */
export interface SmartAlert {
  assetClass: AlertAssetClass;
  kind: AlertKind;
  holding: HoldingRef;
  /** The holding's display name. */
  title: string;
  /** The rule-based verdict. */
  badge: NudgeBadge;
  /** The figure after the verdict, e.g. 'Score 78 / 100'. */
  meta?: SmartAlertMeta;
  /** 2-3 sentences on what the signal means and what to watch, carrying the key figures; LLM-written, one templated sentence as fallback. */
  line: string;
  /** The Ask AI chat message, first person, about this alert. */
  question: string;
}
