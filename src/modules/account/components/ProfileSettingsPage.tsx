"use client";

// Placeholder — replaced by the port of the reference Profile Settings page.
export default function ProfileSettingsPage({ onClose }: { onClose: () => void }) {
  return <div className="absolute inset-0 z-[65] bg-white" onClick={onClose} />;
}
